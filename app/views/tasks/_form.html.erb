<%= form_with(url: task_path, method: "patch")do |f| %>

  <%= f.label :content %>
  <%= f.text_field :content %>

  <%= f.label :task_status %>
  <%= f.text_field :task_status %>

  <%= f.submit %>

<%end%>
